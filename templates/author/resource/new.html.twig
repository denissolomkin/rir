{% extends 'admin/layout.html.twig' %}

{% block body_id 'author_resource_new' %}

{% block main %}
    <h1>{{ 'title.resource.new'|trans }}</h1>

    {% if form in defined %}{{ form(form) }}{% else %}
    {% include 'author/resource/drug-and-drop.html.twig' %}
    <a href="{{ path('author_resource_index') }}" class="btn btn-link">
        <i class="fa fa-list-alt" aria-hidden="true"></i> {{ 'action.back_to_list'|trans }}
    </a>
    {% endif %}

{% endblock %}

{% block sidebar %}
    {{ parent() }}

    {{ (_self) }}
{% endblock %}
