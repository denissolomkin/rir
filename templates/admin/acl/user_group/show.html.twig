{% extends 'base.html.twig' %}

{% block title %}UserGroup{% endblock %}

{% block body %}
    <h1>UserGroup</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_group.id }}</td>
            </tr>
            <tr>
                <th>{{'label.name'|trans}}</th>
                <td>{{ user_group.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_acl_user_group_index') }}">back to list</a>

    <a href="{{ path('admin_acl_user_group_edit', {'id': user_group.id}) }}">{{'action.edit'|trans}}</a>

    {{ include('admin/acl/user_group/_delete_form.html.twig') }}
{% endblock %}
