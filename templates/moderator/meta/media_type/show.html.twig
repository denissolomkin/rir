{% extends 'base.html.twig' %}

{% block title %}ResourceMediaType{% endblock %}

{% block body %}
    <h1>ResourceMediaType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resource_media_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ resource_media_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('resource_media_type_index') }}">back to list</a>

    <a href="{{ path('resource_media_type_edit', {'id': resource_media_type.id}) }}">edit</a>

    {{ include('admin/attribute/resource_media_type/_delete_form.html.twig') }}
{% endblock %}
