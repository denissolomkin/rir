<form method="post" action="{{ path('moderator_meta_media_type_delete', {'id': moderator_meta_media_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ moderator_meta_media_type.id) }}">
    <button class="btn">Delete</button>
</form>
