{% extends 'base.html.twig' %}

{% block title %}ResourceKeyword{% endblock %}

{% block body %}
    <h1>ResourceKeyword</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resource_keyword.id }}</td>
            </tr>
            <tr>
                <th>{{'label.name'|trans}}</th>
                <td>{{ resource_keyword.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('moderator_meta_keyword_index') }}">back to list</a>

    <a href="{{ path('moderator_meta_keyword_edit', {'id': resource_keyword.id}) }}">{{'action.edit'|trans}}</a>

    {{ include('moderator/meta/keyword/_delete_form.html.twig') }}
{% endblock %}
