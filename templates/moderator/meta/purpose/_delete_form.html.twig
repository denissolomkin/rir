<form method="post" action="{{ path('moderator_meta_purpose_delete', {'id': resource_purpose.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ resource_purpose.id) }}">
    <button class="btn">Delete</button>
</form>
