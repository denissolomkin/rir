{% extends 'base.html.twig' %}

{% block title %}ResourceDocumentType{% endblock %}

{% block body %}
    <h1>ResourceDocumentType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resource_document_type.id }}</td>
            </tr>
            <tr>
                <th>{{'label.name'|trans}}</th>
                <td>{{ resource_document_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('moderator_meta_document_type_index') }}">back to list</a>

    <a href="{{ path('moderator_meta_document_type_edit', {'id': resource_document_type.id}) }}">{{'action.edit'|trans}}</a>

    {{ include('moderator/meta/document_type/_delete_form.html.twig') }}
{% endblock %}
