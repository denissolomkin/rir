{% extends 'base.html.twig' %}

{% block title %}ResourcePurpose{% endblock %}

{% block body %}
    <h1>ResourcePurpose</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resource_purpose.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ resource_purpose.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('resource_purpose_index') }}">back to list</a>

    <a href="{{ path('resource_purpose_edit', {'id': resource_purpose.id}) }}">edit</a>

    {{ include('resource_purpose/_delete_form.html.twig') }}
{% endblock %}
